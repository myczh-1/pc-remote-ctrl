智能家居管理平台需求模块

一、总体目标

本项目旨在构建一个基于 gRPC-Web + MQTT 的智能家居管理平台，支持本地自治与云端协同两种模式。平台以“设备影子”为核心抽象，提供设备注册、展示与选择、功能配置、数据反馈等功能。

⸻

二、系统模块划分

1. 本地网关
   •	设备注册
   •	监听设备 provision/announce 消息，校验 PoP（Proof-of-Possession）。
   •	下发正式凭据至 provision/reply/{deviceId}（retained）。
   •	在 UI 上提供“添加设备向导”，支持扫码/输入 ID+PoP。
   •	设备管理（影子管理）
   •	维护 reported / desired 状态，保证设备离线时仍能展示最新状态。
   •	设备上线/掉线通过遗嘱 (LWT) 自动更新。
   •	功能配置/控制
   •	提供 InvokeAction 接口，映射到 MQTT action/{name}/call。
   •	执行结果通过 action/{name}/result 返回，复用现有“命令日志输出”组件展示。
   •	数据反馈
   •	设备上报 shadow/reported、events，网关推送至前端。
   •	可选实现历史存储（SQLite/MySQL），后续扩展为曲线展示。

2. 控制前端
   •	设备展示与选择
   •	设备树/卡片视图，支持房间、类型、状态筛选。
   •	设备功能区
   •	保留命令输入/输出组件，映射到设备动作调用。
   •	提供快捷按钮（如开关、温度调节），本质仍为 action 调用。
   •	状态区
   •	实时展示设备影子 reported 与事件流。

3. 云端中间件
   •	设备管理
   •	提供统一设备目录，支持多用户/多家庭。
   •	维护设备状态，与本地网关保持长连同步。
   •	接口统一
   •	提供与本地一致的 HomeService API。
   •	前端可直连本地网关或云端，调用方式一致。
   •	转发机制
   •	云端转发前端请求到本地网关，保持控制链路一致。
   •	安全
   •	用户账号鉴权、设备凭据下发。
   •	支持用户名/密码或 mTLS 证书。

4. 可延后功能
   •	规则引擎
   •	If-This-Then-That 自动化逻辑，例如“温度 > 28 → 打开空调”。
   •	历史数据存储与可视化
   •	曲线图、报表功能。
   •	内嵌 MQTT Broker
   •	初期可使用 Mosquitto/EMQX，后续可选内嵌轻量 Broker。

⸻

三、核心接口定义（HomeService）
•	RegisterDeviceLocal(deviceId, pop, adapter, meta) → 注册设备
•	ClaimDevice(claimCode) → 云端认领设备
•	ListDevices() / GetDevice(deviceId) → 设备列表与详情
•	GetDeviceShadow(deviceId) → 获取影子状态
•	InvokeAction(deviceId, action, args) → 调用动作
•	TelemetryStream(deviceIds) → 订阅设备事件/状态流

⸻

四、典型时序

1. 注册设备

Device → provision/announce{deviceId,pop}
Gateway/UI → RegisterDeviceLocal
Gateway → 下发凭据 provision/reply/{deviceId}
Device → 重连并上报 shadow/reported, events
UI → 显示设备上线

2. 配置设备

UI → InvokeAction(deviceId,"set_temp",26)
Gateway → 发布 devices/{id}/action/set_temp/call
Device → 执行并返回 result
Gateway → TelemetryStream 推送 action_result
UI → 显示执行结果

3. 状态反馈

Device → shadow/reported{temp=25,power=on}
Gateway → 更新影子并推送 TelemetryEvent
UI → 更新设备卡片状态


⸻

五、阶段目标
•	阶段一（毕设主线）：设备注册、展示、配置、数据反馈。
•	阶段二（扩展功能）：规则引擎、历史数据、内嵌 Broker。