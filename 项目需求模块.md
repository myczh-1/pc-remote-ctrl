lazy-ctrl 功能模块规划文档

## 系统架构概述

lazy-ctrl 采用纯gRPC架构设计，支持两种使用模式：
1. **直连模式**：前端 → gRPC-Web → 本地Controller → 执行命令
2. **云端模式**：前端 → gRPC-Web → 云端中间件 → gRPC → 本地Controller → 执行命令

## 1. 控制器 Agent（Go 实现）- 核心执行引擎
位置：本地电脑，负责执行脚本和提供多协议通信接口。

### - gRPC服务支持：纯gRPC架构
  - 功能目标：
    - **gRPC Server with gRPC-Web**：统一API服务（端口7071）
    - 支持直接gRPC-Web连接（前端直连）
    - 支持标准gRPC连接（云端中间件调用）
    - 统一的命令执行核心

  - 技术实现：
    - 启动时开启gRPC服务器，同时支持gRPC和gRPC-Web协议
    - 统一的GatewayService接口
    - 类型安全的protobuf定义

### - 命令注册/管理：支持注册常用命令或脚本（如 mute、shutdown）
  - 功能目标：
    - 支持用户将常用系统脚本（如 mute、shutdown）注册为命令 ID
    - 支持平台特定命令配置（Windows/macOS/Linux）
    - 提供统一调用接口，所有协议均可使用
    - 支持动态配置热重载

  - 技术实现：
    - 使用本地 JSON 配置文件（`config/commands.json`）
    - 支持嵌套配置结构，按平台区分命令
    - Go 启动时读取并缓存配置
    - 提供配置重载 API

### - 命令执行器：执行本地脚本或系统指令
  - 功能目标：
    - 统一封装系统调用执行入口
    - 支持超时控制和上下文取消
    - 跨平台命令适配（Windows cmd/PowerShell，Unix shell）
    - 输出标准输出/错误并返回执行状态

### - 状态反馈：返回脚本执行结果、输出、是否成功等
  - 功能目标：
    - 标准化响应结构：成功状态 + 输出 + 错误信息 + 执行时间
    - 支持流式输出（长时间运行的命令）
    - 执行日志记录机制

### - 安全限制：多层安全防护机制
  - 功能目标：
    - 命令白名单机制，只允许执行预注册命令
    - 路径隔离，限制脚本执行范围
    - 可选PIN校验，防止未授权访问
    - 执行频率限制，防止恶意调用

---

## 2. 控制前端（React + Vite 实现）
位置：终端控制界面，用户通过Web界面操作本地电脑

### - 连接模式管理：支持两种连接方式
  - 功能目标：
    - **直连模式**：扫码或输入IP直接连接本地Agent（gRPC-Web）
    - **云端模式**：通过云端中间件连接本地Agent（gRPC-Web → gRPC）
    - 连接状态实时显示和自动重连

### - 脚本控制页面：展示当前可用脚本，点击执行
  - 功能目标：
    - 动态获取和展示所有已注册脚本
    - 支持分类展示（系统控制、应用启动、自定义脚本等）
    - 一键执行，支持确认对话框防误操作
    - 支持批量操作和快捷操作

### - 状态反馈：展示执行结果、是否成功、输出日志等
  - 功能目标：
    - 实时展示脚本执行状态与输出反馈
    - 支持长时间任务的进度显示
    - 错误提示与重试机制
    - 执行历史记录查看

### - 配置界面：设备管理和脚本配置
  - 功能目标：
    - 设备连接配置（IP、端口、认证信息）
    - 脚本别名、图标、分组管理
    - 安全设置（PIN码、指纹验证等）
    - 连接偏好设置

---

## 3. 云端中间件服务（Go 实现）
位置：云端代理服务，提供设备管理和gRPC请求转发功能

### - 设备管理（单租户，默认无账户体系）
  - 功能目标：
    - 设备自动注册与在线状态维护
    - 设备分组与标签（可选，后续扩展）

  - 技术实现：
    - 设备UUID生成和管理
    - 内存/Redis缓存设备在线状态
    - 若需公网暴露：可选共享密钥或 mTLS（后续扩展）

### - gRPC代理服务：双向gRPC通信
  - 功能目标：
    - **gRPC-Web服务器**：接收前端客户端请求
    - **gRPC客户端池**：管理到本地Agent的连接
    - 支持连接池和负载均衡
    - 自动重连和故障转移
    - 请求响应日志记录

  - 技术实现：
    - gRPC-Web服务器（兼容浏览器）
    - gRPC客户端连接管理器
    - 心跳检测机制
    - 超时和重试策略
    - 流式调用支持

### - 设备注册与发现
  - 功能目标：
    - 设备自动注册机制
    - 设备状态实时监控
    - 设备路由表维护
    - 多设备负载均衡

### - 统一协议接口
  - 功能目标：
    - 实现相同的GatewayService接口
    - 请求透明转发到目标Agent
    - 支持流式和一元RPC调用
    - 统一的错误处理和状态码

---

## 4. 部署和开发模式

### - 开发模式
  - 本地开发：Go Agent + Go 云端中间件 + 前端同时运行
  - 调试支持：详细日志和错误信息
  - 热重载：配置文件变更自动生效
  - gRPC调试：支持grpcurl和grpc-ui调试工具

### - 生产部署
  - **极简部署**：单个Go Agent二进制文件，支持直连模式
  - **完整部署**：Go Agent + Go 云端中间件 + 前端
  - **混合部署**：本地Agent + 云端中间件，前端可选

### - 跨平台支持
  - Windows/macOS/Linux一键编译
  - ARM架构支持（树莓派、NAS等）
  - Docker容器化部署
  - 自动更新机制

### - 技术栈统一
  - **后端统一**：Controller Agent和云端中间件均使用Go实现
  - **协议统一**：全部使用gRPC/gRPC-Web协议通信
  - **接口统一**：共享相同的GatewayService protobuf定义
  - **类型安全**：protobuf确保前后端类型一致性
